/**
 * sepcial tag function to check if the name to decide whether to do special tags
 *  Include like place_labels, pois(for building poi), contours, public_transport, street_labels/streets, water_lines/water_polygons
 *
 */

/**
 * seperated functions for each special tagging logic with some nice documentation together
 *
 */
