{
  "name": "@basemaps/lambda-api-tracker",
  "version": "3.1.0",
  "private": true,
  "repository": "git@github.com:linz/basemaps.git",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@basemaps/geo": "^3.1.0",
    "@basemaps/lambda": "^3.1.0",
    "@basemaps/shared": "^3.1.0"
  },
  "scripts": {
    "test": "ospec --globs 'build/**/*.test.js' --preload ../../scripts/test.before.js",
    "deploy:bundle": "esbuild --bundle --platform=node src/index.ts --external:aws-sdk --external:pino-pretty --target=es2018 --outdir=dist --format=cjs"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.43",
    "@types/node": "^13.9.8"
  }
}
