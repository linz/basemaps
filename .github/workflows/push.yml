name: Build

on:
  push:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    env:
      NODE_OPTIONS: '--max-old-space-size=8192'
    steps:
      - uses: linz/action-typescript@master

 