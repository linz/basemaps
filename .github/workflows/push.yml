name: Build/Test

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - name: Use Node.js 10.x
      run: docker build . --file Dockerfile --tag basemaps:$GITHUB_SHA-$(date +%s)
